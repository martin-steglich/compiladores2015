package com.language.parser;

import java.util.*;
import java_cup.runtime.*;
import com.language.model.expression.*;
import com.language.exceptions.*;

terminal String ID, INTEGER, LONG, FLOAT, DOUBLE;

terminal String ASSIGN;

non terminal program, expression, module;


program ::= module:list {: RESULT = new Program(list);:};

module ::= expression | expression module;

expression ::= INTEGER | ID ASSIGN expression | DOUBLE | FLOAT| LONG;
